#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
#parse("File Header.java")

import lombok.*;

@AllArgsConstructor
@Getter
@ToString(of = {""})
public enum ${NAME} {
    
    // ATTRIBUTES
  
  
    // METHODS
    getbylibelle
  
}
